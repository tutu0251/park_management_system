#pragma once

// Copy to `backbone_config.local.h` and add `-DBACKBONE_CONFIG_LOCAL` to build_flags.

// #define BACKBONE_ID 1
// #define BACKBONE_RF_CE_PIN 9
// #define BACKBONE_RF_CSN_PIN 10
// #define BACKBONE_RF_CHANNEL 100

// Reader nodes write SWIPE_REQ / STATUS to this address (backbone RX pipe 1):
// #define BACKBONE_RF_ADDR_GW_NODE_INIT 'G','W','A','Y','1'

// Gateway writes downlink to this address (backbone RX pipe 2):
// #define BACKBONE_RF_ADDR_GW_SERVER_INIT 'G','W','S','V','1'

// Backbone writes uplink toward the gateway using this address:
// #define BACKBONE_RF_ADDR_SERVER_INIT 'S','E','R','V','1'

// Disable Serial debug entirely (saves SRAM + flash):
// #define BACKBONE_DEBUG_UART 0

